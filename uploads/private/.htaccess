# Sensitive uploads (ID proofs, etc.) - never served directly by Apache.
# Access only through admin/download.php after an auth check.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
